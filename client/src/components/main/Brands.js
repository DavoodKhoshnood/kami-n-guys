import React from "react";

const Brands = ({ brands }) => {
    // console.log(brands);
    // brands.map((brand) => {
    //     const name = brand.toLowerCase();
    //     const image = `../../assets/img/brands/${name}.png`;
    //     console.log(image);
        return (
            // <img key={brand} src={image} alt={brand} />
            <img src="./assets/img/brands/fiat.png" alt="ggg" />
      );
    // });
};

export default Brands;
